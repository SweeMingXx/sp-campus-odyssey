import test from "node:test";import assert from "node:assert/strict";import {createServer} from "../server.mjs";
test("HTTP serves the game securely and refuses non-runtime files",async()=>{const server=createServer();await new Promise(r=>server.listen(0,"127.0.0.1",r));const base=`http://127.0.0.1:${server.address().port}`;try{for(const path of ["/","/style.css","/src/game.js","/src/data.js","/src/engine.js","/src/world.js","/src/state.js"]){const r=await fetch(base+path);assert.equal(r.status,200,path);assert.equal(r.headers.get("x-content-type-options"),"nosniff");assert.ok((await r.text()).length>500);}for(const path of ["/package.json","/.env","/.github/workflows/ci.yml","/../server.mjs","/%2e%2e%2fserver.mjs","/%ZZ"]){assert.equal((await fetch(base+path)).status,404,path);}assert.equal((await fetch(base,{method:"POST"})).status,405);}finally{await new Promise(r=>server.close(r));}});
